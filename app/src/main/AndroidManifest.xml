<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="qiuhaitao.bwie.com.mall">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:name=".application.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".view.activity.Login_Activity"/>
        <activity android:name=".view.activity.Register_Activity"/>
        <activity android:name=".view.activity.UsercenterActivity"/>
        <activity android:name=".view.activity.Goods_search_Activity"/>
        <activity android:name=".view.activity.GoodsDetailActivity"/>
    </application>

</manifest>